# MkDocs is Python, and this is the only Python in the estate -- everything
# else is Node 24 / Rust. That was a deliberate trade for Material's docs UX;
# the cost is this second toolchain, which devbox contains rather than
# spreading into the machine.
eval "$(devbox generate direnv --print-envrc)"
